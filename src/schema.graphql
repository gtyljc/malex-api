
type Query {
    works: [Work],
    appoitments: [Appointment]
}

enum BestWayToTouch {
    ASSEMBLING,
    MOUNTING,
    PLUMBING
}

type Appointment {
    id: Int!,
    name: String!,
    surname: String!,
    address: String!,
    job_desc: String!,
    bwt: BestWayToTouch
}

type Work {
    id: Int!,
    img_urls: [String]!,
    img_id: ID,
    category: String!
    timestamp: 
}